# Apollo Platform API

Internal service module for Platform, nginx, and Certbot. The `local` and `vps`
roots supply the shared Docker network, PostgreSQL/PgBouncer, Redis, certificates,
database roles, migrations, OAuth records, DNS, and secrets.
